[package]
name = "wasm_decrypt"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
aes-gcm = "0.10"
sha2 = "0.10"
getrandom = { version = "0.2", features = ["js"] }

[profile.release]
opt-level = "z"
lto = true